{
  "name": "mirrorpc",
  "version": "1.0.0",
  "private": true,
  "description": "Secondo schermo wireless via WebRTC per PC, iPad e Android",
  "main": "electron-main.js",
  "scripts": {
    "start": "electron .",
    "start:server": "node server.js",
    "check": "node --check server.js && node --check electron-main.js && node --check electron-preload.js && node --check public/host.js && node --check public/viewer.js && node --check public/dashboard.js && node --check public/public-portal.js",
    "test:signaling": "node tests/signaling.test.js"
  },
  "dependencies": {
    "electron": "^44.0.0",
    "express": "^5.1.0",
    "jsqr": "^1.4.0",
    "qrcode": "^1.5.4",
    "qrcode-generator": "^1.4.4",
    "ws": "^8.18.3"
  }
}
