{
  "name": "mirrorpc",
  "version": "1.0.0",
  "private": true,
  "description": "Secondo schermo wireless via WebRTC per PC, iPad e Android",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/host.js && node --check public/viewer.js && node --check public/dashboard.js && node --check public/public-portal.js",
    "test:signaling": "node tests/signaling.test.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "jsqr": "^1.4.0",
    "qrcode": "^1.5.4",
    "qrcode-generator": "^1.4.4",
    "ws": "^8.18.3"
  }
}
